<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title><%= context.name %></title>
  </head>
  <body>
    <center>
    <h1><%= context.name %></h1>
    <img src="/<%= context.img %>" alt="">
    <h2>Favorite food: <%= context.food %></h2>
    <h2>Age: <%= context.age %></h2>
    <h2>Favorite places to sleep:</h2>
      <% for (var i = 0; i < context.sleep_spots.length; i++) {%>
        <%= context.sleep_spots[i] %><br>
      <% } %>
  </body>
</html>
